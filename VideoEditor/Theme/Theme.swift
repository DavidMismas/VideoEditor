import SwiftUI

enum Theme {
    static let backgroundDark = Color(red: 0.05, green: 0.08, blue: 0.15)
    static let panelBackground = Color(red: 0.1, green: 0.12, blue: 0.2)
    static let accentPink = Color(red: 0.95, green: 0.35, blue: 0.6)
    static let accentGreen = Color(red: 0.2, green: 0.85, blue: 0.5)
    static let textMain = Color.white
    static let textSecondary = Color(white: 0.7)
    static let separator = Color(white: 0.2)
}
